import java.util.Scanner;

public class problem6 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        /*
         * PROBLEM 6: Prime Number Checker
         * -------------------------------
         * A Prime Number is a number greater than 1 that can only be divided
         * by 1 and itself (e.g., 2, 3, 5, 7, 11...).
         * 
         * Your Task:
         * 1. Input: Ask the user for an integer N.
         * 2. Logic: Check if N is prime.
         * - If N <= 1, it's NOT prime.
         * - Use a loop to check if N is divisible by any number from 2 to N/2 (or
         * sqrt(N)).
         * 3. Output: Print "N is a Prime Number" or "N is not a Prime Number".
         */

        System.out.print("Enter a number to check: ");
        int N = sc.nextInt();

        // YOUR CODE HERE

    }
}
